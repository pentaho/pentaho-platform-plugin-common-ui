{
  "name": "${project.amd-module}",
  "version": "${project.version}",
  "config": {
    "pentaho/service": {
      "${project.amd-module}_${project.version}/config": "pentaho.config.spec.IRuleSet"
    }
  }
}
