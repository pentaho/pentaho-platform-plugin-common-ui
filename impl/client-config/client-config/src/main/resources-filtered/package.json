{
  "name": "${project.amd-module}",
  "version": "${project.version}",
  "paths": {
    "pentaho/config/deploy": "/"
  },
  "config": {
    "pentaho/modules": {
      "${project.amd-module}@${project.version}/pentaho/config/deploy/config": {"type": "pentaho/config/spec/IRuleSet"}
    }
  }
}
