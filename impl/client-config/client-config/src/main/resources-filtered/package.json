{
  "name": "${project.amd-module}",
  "version": "${project.version}",
  "config": {
    "pentaho/modules": {
      "${project.amd-module}_${project.version}/config": {"type": "pentaho/config/spec/IRuleSet"}
    }
  }
}
