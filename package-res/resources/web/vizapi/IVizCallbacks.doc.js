/**
 * @module common-ui.vizapi
 */

/**
 * This interface is a **documentation artifact** that
 * contains methods representing **callbacks** used
 * in other parts of the API.
 *
 * Unless otherwise noted, the describe methods are called on the global scope,
 * i.e., with `this` ebing the global object.
 *
 * @class IVizCallbacks
 * @constructor
 */

/**
 * Constructs a a visualization instance of the given type
 * and with the given options.
 *
 * @method instanceFactory
 * @param {String} type The type of visualization instance to construct.
 * @param {IVizCreateOptions} options The construction-time visualization options.
 */
