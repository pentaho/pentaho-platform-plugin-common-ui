/**
 * @module common-ui.vizapi
 */

/**
 * The visualization creation options interface is a **documentation artifact** that
 * describes configuration options, that
 * control the presentation and behavior of visualizations,
 * and that do not change through the lifetime of a visualization instance.
 *
 * It is the type of the
 * `options` argument of a visualization instance factory method.
 *
 * @class IVizCreateOptions
 * @constructor
 */

/**
 * The DOM element into which the visualization should render.
 * The element is initially empty.
 *
 * @property domNode
 * @type HTMLElement
 * @since 3.0
 */
